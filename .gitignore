tmpclaude-*-cwd
nul
supabase/debuggingData.txt
fixes.txt
tools/.bad-*.js
tools/.debug-*
# default output dir for tools/dump-dojo-content.js (curriculum review dumps)
_dojo_dump/

# OS / editor / tooling junk
.DS_Store
**/.DS_Store
.claude/
supabase/.temp/

supabase/migrations/*.rtf
# Backend migration map + its generator live in the PRIVATE backend repo.
# Never commit the map here: this repo is public and Pages serves every file.
MIGRATION_MAP.md
